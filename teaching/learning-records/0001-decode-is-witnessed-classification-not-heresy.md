# The Realization-facing decode is a witnessed classification problem, not an assertion of meaning

Scott now holds the distinction that made the decode *feel* hard: freezing a raw object-table word does **not** make it portable — a word whose *value is a PDP-1 machine artifact* (e.g. `mtb` = `002310`, a calc-routine pointer, rewritten to `mex`'s address on explode) stays **reference-internal** until a **witnessed decode** gives it a game-side end. He also corrected the collapse that produced the "am I crazy?" fog: the ADRs litigated *capture* to death but **consciously deferred** *decode* (`oracle/reference-snapshots.js:13`). This matters because it retires the false whiff of heresy — a mapping read off Substrate execution is `observe-don't-assert` one layer up, not Translator-class interpretation — and unblocks a go/no-go on phase 2.

**Evidence.** Unprompted, Scott (a) *widened* the `Variant` definition himself to "any deviation," surfacing the **non-observable Variant** and its sharp consequence — the goldens can only *falsify* Exactness, never *prove* it (the ProgramBench "finite tests are a lower bound" point promoted into the term); (b) drove the `002310` reference-internal reasoning correctly; (c) rewrote `CONTEXT.md` to make **Exact Realization / Variant** an exhaustive partition and deprecated the old "Golden Realization" name.

**Implications for next session.** Knowledge has landed; the remaining work is *decisions*, not teaching. Resume via `/grill-with-docs` on the two genuinely-open questions only: (1) is "same" **exact / no-epsilon**? (2) is there a third **"drop" bin** for zero-game-content words like `nb1` (the `mtc` instruction-budget count, `:677`)? Then the per-field A/B/C ledger (needs EPIC #5 per-routine read/write semantics). Do **not** re-teach the fact/encoding weld or witnessed-decode principle — they are established. See [[GLOSSARY.md]]; grounds the phase-2 go/no-go in [[MISSION.md]].
